services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:80"
    volumes:
      - ./db:/app/data
    environment:
      - NODE_ENV=production
      - PORT=80
      - DB_PATH=/app/data/lotek.db
    networks: [lotek]
networks:
  lotek:
